{
    "name": "interviewai",
    "type": "module",
    "private": true,
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
        "start": "npm --prefix backend start",
        "lint": "npm --prefix frontend run lint",
        "build:backend": "npm --prefix backend run build",
        "build:frontend": "npm --prefix frontend run build",
        "build": "npm run build:backend && npm run build:frontend && npm run bundle:api",
        "bundle:api": "npx esbuild backend/src/serverless.ts --bundle --platform=node --packages=external --outfile=api/index.js --format=esm",
        "check:prod": "npm run build:backend && npm run lint && npm run build:frontend && npm run bundle:api"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "esbuild": "^0.25.0"
    },
    "dependencies": {
        "axios": "^1.7.9",
        "bcryptjs": "^2.4.3",
        "body-parser": "^1.20.3",
        "cookie-parser": "^1.4.7",
        "cors": "^2.8.5",
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "express-validator": "^7.3.1",
        "jsonwebtoken": "^9.0.2",
        "mongoose": "^8.10.1",
        "multer": "^2.0.2",
        "openai": "^4.85.4",
        "pdf-parse-fork": "^1.2.0",
        "serverless-http": "^4.0.0"
    }
}
